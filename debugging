def calculate_harmonic_mean( ):
    n = len(nums)
    reciprocal_sum = sum(1 / num for num in nums)

# Test the function
print("Harmonic mean:", calculate_harmonic_mean([2, 4, 6, 8]))def factorial(n):
   
def factorial(n):
    result = 1
    for i in range(1, n + 1):
        result = i
    

# Test the function
print("Factorial of 5:", factorial(5))

def tuple_operations(t1):
    return t1 + t2

# Test the function
tuple1 = (1, 2, 3)
tuple2 = (4, 5, 6)
print("Concatenated tuple:" tuple_operations(tuple1, tuple2))

def is_prime( ):
if n <= 1:
        return False
    for i in range(2, n):
        if n % i == 0:
            return False
    return True

# Test the function
print("Is 17 prime?", is_prime(17))
